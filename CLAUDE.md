# 机械臂视觉抓取项目 · Agent 必读

> 这份文件每个新会话自动载入。它只放**不知道就会造成损失**的事，细节在文末索引。
> 现场经验来自 2026-08-22 的调试（当天摔了一次机械臂），结论都有日志证据。

## 设备

MOKA 藦卡 MR07S-930（6 轴）+ **纳博特 Inexbot C1102** 控制器，固件 `21.05.23-4.1.1`。
控制器 `192.168.1.200`，示教器 `192.168.1.245`。相机 OAK-D-PRO。

---

## 四条会造成损失的事实

### 1. `0x2314` 不是急停，是**直接下电**

实测 5/5 次映射到 `Deadan_End -> PowerOff`。伺服失力，**伸展着的手臂会靠自重坠落**——
2026-08-22 摔臂就是控制器自己触发的 PowerOff。

- 真正的安全急停只能是**示教器上的物理按钮**
- 运动还没下发时不要"保险起见"发它，那是纯粹的自伤
- 发过之后伺服断电，下一条运动必须重新 `0x2311` 使能

### 2. 远程模式下**每条运动都会被拒，并且拒绝会顺手把带电的伺服下掉**

出厂配置 `global.json` 的 `RemoteIO[0].posReset` 里 `safeEnable=true` 但逐轴容差
`deviation=null`，于是复位点安全闸门恒判"不在安全位置"——机器人精确停在复位点、
逐轴只差 1e-5° 也照样被拒。

- 实测（2026-08-23 用 `read_controller_logs.py` 全量复核 80 万条归档，
  并用 `zcat|grep` 独立对过）：**08-22 当天被拒 18 次**（归档里共 20 条，
  另 2 条属 08-19 旧会话）。**全部 18 次都在远程模式，全部立刻走 `Deadan_End` 停止链**；
  其中 **2 次** `Robot_Group_State=1`（伺服带电）→ 真的执行了 `PowerOff`，
  另 16 次 `=0`（本来就没电）→ 没有可下的电。
  所以准确说法是**"拒绝必走停止链；当时带电就把电下掉"**，
  而不是"每拒一次必下电一次"——但**操作后果一样**：你为了运动刚使能完，
  一被拒就掉电。
- 闸门只咬 `0x3002`/`0x3003`/`0x3007` 这三条；`0x4501`/`0x4502`
  在远程模式下先被 `0x2B03` 参数错误挡掉，根本走不到闸门。
- 示教模式下真正跑起来的是 **`0x4502` MOVL**；**`0x4501` MOVJ 参数错误率极高**
  （归档里 0x4501 参数错误 81 条 vs 0x4502 21 条）。
  **我们的执行链全程只用 MOVL**（`move_to_ucs` → `move_l`），不要往里加 MOVJ。
- 症状是"机械臂完全不动，但夹爪照常开合"（夹爪走 `0x3601` DOUT，是另一条码路）
- **对策：全程留在示教模式**

### 3. A/B/C 的单位：回读是**度**，运动指令收**弧度**

跨这个边界不转换会摔臂（2026-08-22 实际发生）：回读到 `(177.87, 13.76, -179.99)` 度
被当弧度发出，姿态要转 119.6° 而 XYZ 只走 30mm → 六轴同时 0F15 → 下电 → 坠落。

- `current_pose()` 返回**度**，`current_pose_rad()` 返回**弧度**
- `move_to_ucs()` 收**弧度**，`check_abc_is_radians()` 会拦截度数值
- 旋转约定是内旋 X'Y'Z' 即 `R = Rx(A)·Ry(B)·Rz(C)`，**不是**固定系 `Rz·Ry·Rx`

### 4. `tool/arm_project_20260822/` 是**分叉的旧副本**，不要从里面启动任何东西

那是视觉队友 U 盘拷来的另一个 git 仓库，与主仓在 `c3d831c` 分叉。
它缺今天的全部修复。启动脚本用自身位置推 `PROJECT_ROOT`，从那里启动会整个绕过主仓。
保留它只为回退参照。

---

## 环境（每个新终端）

```bash
cd /home/throne/workspaces/arm
export ARMPY=/home/throne/miniconda3/envs/foundationpose/bin/python
export PYTHONPATH=$PWD:$PWD/ros_ws/src/arm_vision_framework/src
```

系统 `python` 是 3.8，没有 trimesh/torch。用它跑自检会**静默跳过**关键检查
（已改成显式报错，但仍要用对解释器）。

## 跑测试

```bash
$ARMPY -m unittest discover -s competition_pipeline/tests -t . -p 'test_*.py'
$ARMPY -m unittest discover -s tool/visual_grasp_pipeline/tests -t . -p 'test_*.py'
cd ros_ws/src/arm_vision_framework && PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

## 无硬件验证（不需要机械臂/相机/GPU）

```bash
./competition_pipeline/scripts/run_offline_rehearsal.sh            # 全栈演练
./competition_pipeline/scripts/run_offline_rehearsal.sh --fault servo-refuse   # 故障演练
$ARMPY -m competition_pipeline.scripts.check_vision_assets         # 视觉资产自检
```

---

## 坐标系与抓取约定

**用户坐标系1**：原点 = 49.3cm 方桌的**桌面中心**，`+X` 前方、`+Y` 左方、`+Z` 上方，
**`z=0` 就是桌面**（物体的 Z 可直接读成"离桌面多高"）。桌面范围 ±246.5mm。

**抓取高度**（夹爪腔体深度实测 80mm）：

```
瓶/罐 (cylinder)   z = (顶点 + 中心)/2     -> 伸入 = 高度/4
水果   (sphere等)   z = 中心
统一钳位 伸入 <= 腔体深度 - 安全余量(65mm)
```

对准物体**中心**会要求伸入 `高度/2`，高于 160mm 的瓶子必然被掌根压爆
（隔壁组已发生）。"中心"必须取**包围盒中心**而非网格原点——apple 的原点偏 49.1mm、
nescafe 偏 74.5mm，用原点会把夹爪怼进桌面。

---

## 遇到没见过的通信问题

**不要查手册和网站。** 公开的 22.07 文档漏了 `acc`/`dec` 必填字段、`coord=3` 的含义
也与实际不符；已有 7 份基于手册的文档被推翻并改成了反例。

用这三条地面实况：控制器自己的日志 / 我们这端的帧记录 / `0x2B03` 报警文本。
详见 `docs/通信问题现场定位手册.md`。

---

## 文档索引（按该读的顺序）

| 文档 | 内容 |
|---|---|
| `docs/明天验收-现场执行清单.md` | **现场按这个走**，含故障排查与不该做的事 |
| `docs/纳博特C1102-现场真相-必读.md` | 协议与控制器行为的权威结论，每条标了证据等级 |
| `docs/通信问题现场定位手册.md` | 通信问题怎么定位（三条地面实况渠道） |
| `docs/现场备份-20260822/根因证据-控制器日志摘录.txt` | 原始日志证据，可 grep |

**证据等级约定**：文档里的结论标注【实测】/【配置】/【推断】/【存疑】。
不要把【推断】当【实测】用，也不要在没有证据时替代码"圆场"。

---

## 工作方式

- 这是竞赛项目，**没有试错余量**。改动要能被离线验证（有 380+ 项测试）。
- 提出结论要说清是实测还是推断。之前有过把推断当结论汇报、被交叉复核纠正的情况。
- 安全相关的改动宁可拒绝执行也不要"尽力而为"——现场最贵的失败是
  "程序说成功了但机器人没动"，以及"程序没说话就把东西压爆了"。
