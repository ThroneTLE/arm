# Runtime dependencies for the visual grasping pipeline.
# Use the existing `foundationpose` conda environment; this list is for reference.
numpy>=1.24
opencv-python>=4.8
ultralytics>=8.0
trimesh>=4.0
PyYAML>=6.0
scipy>=1.9
pyzmq>=25.0
Pillow>=10.0
matplotlib>=3.7
# FoundationPose vendor checkout and CUDA are configured outside the repo.
