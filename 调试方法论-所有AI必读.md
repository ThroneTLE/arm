# 调试方法论 · 所有 AI 必读

> **无论你是 Claude / Codex / DeepSeek / Gemini / Cursor，接手这个项目先读完这一页。**
>
> 这不是"项目介绍"，是**九条用真实代价换来的调试纪律**。每一条后面都跟着它是怎么被
> 学到的——2026-08-22 那天我们摔了一次机械臂、被赶出场地、隔壁组压爆了一个瓶子。
>
> 具体的设备结论看 [`CLAUDE.md`](CLAUDE.md)；这里讲的是**方法**。

---

## 1. 手册说的是"这类设备应该怎样"，日志说的是"这台设备实际怎样"

冲突时**永远以日志为准**，而且要在文档里写清是谁说的。

> **代价**：公开的 RTL-22.07 文档漏了 `acc`/`dec` 两个必填字段——不加就返回
> `指令[0x4502]参数错误`。我们照着文档试了一整天运动指令，全部被拒。
> 后来从控制器日志里看到解析顺序是 `robot→vel→acc→dec→coord→pos[7]`，加上就通了。
> 同一份文档里 `coord=3` 的含义也与实际不符。
>
> 最终有 **7 份基于手册写的项目文档被推翻**，就地改成了反例（原文保留 + 删除线 +
> ❌已推翻标注），专门留给后来的 agent 看，免得再走一遍。

**推论**：在这个项目里，"我查到手册说……"不构成证据。要么给日志行号，要么标成【推断】。

---

## 2. "没有报错"不等于"成功"

必须有**肯定的成功证据**，而不是"没看到失败"。

> **代价**：控制器拒绝一条运动指令有三种表现——回一个报警帧、回一个伺服状态帧、
> 或者**什么都不回**。三种情况下机器人的位姿都不变。而旧代码判断"运动完成"的方式是
> **"位姿不再变化"**——这个条件在三种拒绝下都立刻成立。
>
> 于是程序连续报告"运动成功"，机器人一动没动，我们对着屏幕查了几个小时。
>
> 修法：等控制器主动推送 `0x3D03 status=2`（真的开始动了）才算成功。

**推论**：任何"完成/成功"的判定，先问一句——**这个条件在失败时会不会也成立？**

---

## 3. 静默降级比直接失败更危险

跳过的检查会给**假信心**。

> **代价（当晚差点又中一次）**：赛前自检脚本在缺 `trimesh`/`torch` 时会打印
> `⚠️ 跳过尺寸检查`，然后照常输出一份"只有 1 个已知问题"的报告。而被跳过的恰好是
> 防"CAD 缩放错→算错抓取高度→压爆物体"和"权重类别改名后静默把可乐瓶当雪碧罐"的
> 那两道。用系统 python 跑一次就会看到一份**看起来挺正常**的报告。
>
> 已改成显式失败并提示该用哪个解释器。

**推论**：诊断工具缺依赖时要**失败**，不要"跳过"。降级路径必须比正常路径更吵。

---

## 4. 同一个数学约定，只能有一份实现

第二份迟早会写错，而且错得很隐蔽。

> **代价**：`scripts/fp_to_user1.py` 自带了一份 A/B/C 姿态逆解，用 `Rz·Ry·Rx` 的公式
> 去逆 `Rx·Ry·Rz`——**随机姿态往返最坏偏 179.97°**。它有 `--self-test`，但自检用的是
> **单位旋转**，所以永远测不出方向性错误。这份代码在仓库里躺了很久没人发现。

**推论**：发现重复实现就合并到唯一实现；不能合并就至少写一个"两份必须一致"的测试。

---

## 5. 自检要用**能失败**的输入

对称输入测不出方向性错误。

> 单位旋转、零向量、正方形、相等的边长——这类输入会让"顺序错了/符号反了/轴搞混了"
> 全部通过。上面第 4 条就是被单位旋转放过去的。
>
> 现在的测试用的是：真实现场姿态、非单位旋转、各轴不等长的包围盒、偏航 45°
> （因为轴对齐包围盒在 45° 时会把直径放大 41%）。

**推论**：写完测试问一句——**如果我把这段代码的某个符号翻过来，这个测试会挂吗？**

---

## 6. 单位和参考系的**跨边界处**必须有闸门

那是这个项目最贵的错误来源，没有之一。

> **代价一（摔臂）**：回读给的 A/B/C 是**度**，运动指令收的是**弧度**。UI 上有个
> "用弧度"复选框，勾上之后 `(177.87, 13.76, -179.99)` 度被当弧度发出——姿态要转
> 119.6°，而 XYZ 只走 30mm，六轴同时 0F15 故障 → 控制器下电 → 机械臂坠落。
> 那个复选框已被**永久删除**。
>
> **代价二（差点怼桌面）**：抓取点取的是"物体位姿的平移"，也就是**网格原点**。
> 但 apple 的网格原点在果底下方，偏包围盒中心 49.1mm，nescafe 偏 74.5mm。
> 按原点抓苹果会往桌面下方伸 44mm。

**推论**：函数名/参数名带上单位（`_mm`、`_rad`、`_deg`）；在边界上放主动拦截的闸门，
不要只写注释。闸门的报错要说清**是什么错、该怎么改**。

---

## 7. 改一个约定，要找齐**所有消费端**

漏一个就是系统性偏差，而且不会报错。

> **代价**：有个提交把 A/B/C 的旋转约定从 `Rz·Ry·Rx` 改成 `Rx·Ry·Rz`，改了
> geometry / tcp_pose / hand_eye / adapter 四处——**漏了 `ui.py` 里的一个消费端**。
> 结果手眼标定采样系统性偏 4°~15°，没有任何报错。

**推论**：改约定时用 grep 找**全部**调用点，并且在唯一实现处写清"两种约定不可互换"。

---

## 8. 安全相关的分支，宁可**拒绝执行**也不要"尽力而为"

> **代价**：`PickPlaceExecutor` 在 dry-run 模式下拒绝执行时会调用 `robot.stop()`
> "保险起见"。但在这台控制器上 `stop()` 发的是 `0x2314`，实测映射到
> `Deadan_End -> PowerOff`——**直接下电**。也就是说，一次纯粹的空跑调用会把伸展着的
> 机械臂断电，让它自重坠落。
>
> 现在：联锁在运动下发**之前**拒绝时，绝不发 stop。

**推论**：在"信息不足"和"尽力试试"之间，选前者。缺物体尺寸就不许抓（无法保证不压爆），
缺伺服确认就不许发运动。**没做**的成本远低于**做错**。

---

## 9. 报错信息要能让**现场的人**照着做

三小时的调试窗口里，一条含糊的报错能烧掉半小时。

> 对比一下：
>
> - ❌ `执行抓取失败: None` ← 真实发生过。原因是 `str(None) == "None"` 是真值，
>   把提取报警文本的逻辑整段变成死代码，所有报警都显示成 `None`。
> - ✅ `物体夹持宽度 95.0mm 超过夹爪可用张开 79.0mm。这是**机械限制不是软件问题**，
>   请换目标物体或换夹爪。`
> - ✅ `0x2311 上使能后伺服仍为 status=1(需要 3=运行)。现场实测主因: 复位点安全闸门
>   在远程模式下拒绝每条指令并下电。对策: 示教器切回示教模式。`

**推论**：报错里写清**是什么、为什么、现在该做什么**。区分"机械限制"和"软件 bug"——
这两者现场的处理方式完全不同。

---

## 写文档时的证据分级（强制）

所有结论必须标注来源等级，**不标就等于骗人**：

| 标记 | 含义 |
|---|---|
| 【实测】 | 有日志/抓包/实机验证，附证据位置 |
| 【配置】 | 从设备配置文件读出（如 `global.json`） |
| 【推断】 | 逻辑推理得出，**没有直接验证** |
| 【存疑】 | 有矛盾证据或来源不可靠 |

> **代价**：我曾把"把 `deviation` 填成 1.0°"作为修复步骤给出去，语气像是结论。
> 交叉复核指出：这只是从同配置文件里另外三台**没启用**的机器人抄来的数字，
> **从未验证过**。已降级为"未验证的候选修复"。
>
> 唯一【实测】可用的路径是：**全程留在示教模式**。

---

## 接手时的最短路径

1. 读 [`CLAUDE.md`](CLAUDE.md)——四条会造成损失的事实 + 环境设置
2. 跑一次无硬件全栈演练，确认你的改动没破坏链路：
   ```bash
   ./competition_pipeline/scripts/run_offline_rehearsal.sh
   ```
3. 遇到通信问题看 [`docs/通信问题现场定位手册.md`](docs/通信问题现场定位手册.md)，
   **不要去查手册和网站**（见第 1 条）
4. 改完跑全部测试（380+ 项，命令见 `CLAUDE.md`）

## 不要做的事

- 不要从 `tool/arm_project_20260822/` 启动任何东西（分叉的旧副本，缺全部修复）
- 不要用软件"急停"当安全急停（`0x2314` 是下电，手臂会掉）
- 不要因为"看起来没问题"就把【推断】写成结论
- 不要在没有离线验证手段的情况下改运动链路
